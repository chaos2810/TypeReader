<Window x:Class="TypeReader.App.WidgetWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        Title="Type Reader" Width="120" Height="48"
        WindowStyle="None" ResizeMode="NoResize" ShowInTaskbar="False"
        Background="Transparent" AllowsTransparency="True"
        TextOptions.TextRenderingMode="ClearType" TextOptions.TextFormattingMode="Ideal">
    <Grid>
        <!-- R3: transparent surface; acrylic blur is applied to the HWND in
             TaskbarEmbedder.ApplyAcrylic(). Background stays transparent so
             the blur shows through; rounded corners per FluentFlyout.
             Near-invisible background makes the full area hit-testable. -->
        <Border x:Name="MainBorder" CornerRadius="6" Margin="2" Background="#01000000"
                MouseEnter="OnMouseEnter" MouseLeave="OnMouseLeave">
            <Border x:Name="TopBorder" BorderThickness="0,1.25,0,0" CornerRadius="5"
                    BorderBrush="Transparent">
                <TextBlock x:Name="KeyText" Text="…" FontFamily="Segoe UI" FontSize="14"
                           Foreground="White" VerticalAlignment="Center"
                           HorizontalAlignment="Center" Padding="8,2">
                    <TextBlock.ContextMenu>
                        <ContextMenu x:Name="KeyMenu">
                            <ui:MenuItem Header="Settings" Click="OnSettings">
                                <ui:MenuItem.Icon>
                                    <ui:SymbolIcon Symbol="Settings24"/>
                                </ui:MenuItem.Icon>
                            </ui:MenuItem>
                            <Separator/>
                            <ui:MenuItem Header="Quit" Click="OnQuit">
                                <ui:MenuItem.Icon>
                                    <ui:SymbolIcon Symbol="Dismiss24"/>
                                </ui:MenuItem.Icon>
                            </ui:MenuItem>
                        </ContextMenu>
                    </TextBlock.ContextMenu>
                </TextBlock>
            </Border>
        </Border>
    </Grid>
</Window>